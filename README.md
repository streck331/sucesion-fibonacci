# sucesion-fibonacci
escribir "escribe un numero"
leer numero
anterior<-0
siguiente<-1
	Para i<-1 Hasta n Hacer
		Escribir anterior
		resultado<-anterior + siguiente
		anterior<-siguiente
		siguiente<-resultado
	FinPara
